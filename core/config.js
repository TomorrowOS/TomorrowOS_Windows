/**
 * Windows player boot configuration.
 * Written/overwritten by TomorrowOS.Setup during install.
 * Edit manually for lab installs if needed.
 */
window.TOMORROWOS_CONFIG = {
  /** HTTP or HTTPS CMS URL (converted to ws:// / wss:// at runtime). */
  cmsEndpoint: "https://testcms2.replit.app",
  /** landscape | portrait-right | portrait-left */
  orientation: "landscape",
  /** contain | cover | stretch — how media fills the playback surface */
  contentFit: "cover",
  /** Optional preferred monitor index from installer */
  displayIndex: 0
};
